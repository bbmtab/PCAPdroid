## Latest session — 2026-08-01 — Item (c) source-verification pass: adblock SYN-time-vs-post-parse architectural lock RE-VERIFIED against `pcapdroid.c` HEAD `05762935`; plan.md line-number citations DRIFT-CORRECTED (doc-only, no code, no commit/push)

> **Task scope:** lead's explicit context-gathering pass BEFORE any design discussion of item (c) (fixing the post-parse-only timing race). Three Item-(c) verification asks: (1) confirm `check_domain_block_rules` and `check_adblock_sni_rules` current state with NO SYN-time adblock consult silently reintroduced; (2) confirm revert rationale comment present; (3) re-verify `data->info` (DNS-LRU, IP-keyed) vs `host_server_name` (nDPI) divergence at exact call sites. **No fix proposed** — design decision (post-parse-only) is with the lead; this is reference gathering only.

> **Verification result — all three asks CONFIRMED against source** (`app/src/main/jni/core/pcapdroid.c` on `debug-emulator` HEAD `05762935`):
> - **(1) No SYN-time adblock reintroduction.** `check_domain_block_rules` body ([pcapdroid.c:300-361](../app/src/main/jni/core/pcapdroid.c#L300-L361)) consults ONLY `pd->malware_detection.bl` (:302-319) and `pd->firewall.bl` (:322-342) — **zero `pd->adblock.*` reference**. `check_adblock_sni_rules` body ([pcapdroid.c:562-595](../app/src/main/jni/core/pcapdroid.c#L562-L595)) is post-parse-only (`process_ndpi_data` call sites :622 TLS / :643 HTTP / :660 post-overwrite re-call; `pd_new_connection` consult at :479). The `!data->to_block` precedence guard sits at :570, bypass-allowlist at :572, blocklist at :576. Architecture is unregressed — adblock is NOT consulted at SYN.
> - **(2) Revert rationale comment PRESENT.** 16-line comment block at [pcapdroid.c:344-359](../app/src/main/jni/core/pcapdroid.c#L344-L359), at the END of `check_domain_block_rules`'s body inside the `if(data->info && data->info[0])` outer guard but AFTER both the malware and firewall blocks (which close at :319 and :342). Substantive content (three named reasons) unchanged from plan.md's "Closed investigation 2026-07-12" record.
> - **(3) `data->info` vs `host_server_name` divergence RE-VERIFIED at call sites.** SYN-time IP-keyed LRU read [pcapdroid.c:435](../app/src/main/jni/core/pcapdroid.c#L435) `data->info = ip_lru_find(pd->ip_to_host, &dst_ip);` (inside `pd_new_connection`); DNS-derived LRU write [pcapdroid.c:828](../app/src/main/jni/core/pcapdroid.c#L828) `ip_lru_add(pd->ip_to_host, &rsp_addr, query);` (`query` from `data->ndpi_flow->host_server_name` at :762, inside `process_dns_reply`). **Primary-source divergence proof** at [pcapdroid.c:654-658](../app/src/main/jni/core/pcapdroid.c#L654-L658): when nDPI provides `found_info` and `data->info` came from LRU (`data->info_from_lru`), PCAPdroid OVERWRITES `data->info` — nDPI > LRU priority — the two sources are DISTINCT.

> **Three named reasons the post-parse-only lock stands (per `pcapdroid.c:344-359` inline comment):** (a) `capture_vpn.c` drops SYN synchronously after `zdtun_lookup` — SYN drop is UNCORRECTABLE; (b) `check_adblock_sni_rules` is the ONLY `@@`-exemption path gated on `!data->to_block` — a wrong at-SYN block would suppress the post-parse allowlist FOREVER; (c) `recompute_conn_block_cb` housekeeping reset is a NO-OP for adblock-sourced blocks (`blacklisted_domain` has no adblock-side clear path; only the malware whitelist-reload handler clears it).

> **Tracked-open architectural limitation (NOT a defect introduced here, NOT fixable by this architecture):** the post-parse `process_ndpi_data` runs AFTER the data-carrying packet transits → nDPI classifies → `host_server_name` populated. But `capture_vpn.c` checks `if(data->to_block) drop` AFTER `pd_process_packet`, so a HTTP GET may already be in flight and the server's 200 may already be returned before the block fires. Anchor test `testAdblockGateArmedFiresOnEarlyDrop` fails at `AdbyeE2ETest.java:521` byte-identically both pre- and post-revert — KNOWN, NAMED, TESTABLE gap; NOT a regression.

> **Constraint #2 cardinal risk (why the lock is load-bearing):** `data->info` is IP-keyed DNS-LRU hostname; on shared CDN IPs (Cloudflare/Akamai/Google edge) it can MISMATCH real SNI/HTTP Host (stale LRU entry, different domain on same edge IP). Consulting adblock at SYN on `data->info` would risk silently dropping Play Store / media CDN / GMS push (constraint #2 Resource Protection — MUST NEVER be silently dropped) on the miskey. Post-parse consult on real `host_server_name` avoids this.

> **Documentation drift corrected this pass (doc-only — plan.md, no `.c`/`.java` changes, no commit/push):** plan.md's Item-(c) section line-number citations had drifted from later inter-checkpoint commits on `debug-emulator`. Six edits re-aligned citations to current source while PRESERVING all prose reasoning: `:302→:300` (`check_domain_block_rules` def); `:547→:562` (`check_adblock_sni_rules` def); `:557→:572` / `:561→:576` (allowlist/blocklist cites); `:607→:621-622` / `:628→:630-643` (`process_ndpi_data` post-parse consults); "former `pcapdroid.c:391-392`"→`:344-359` (revert rationale cite). Update note "Line-number citations updated 2026-08-01" appended at the end of plan.md's Closed investigation section.

> **cmd-runner execution note:** Item-(c) ask-1 and ask-3 cmd-runner agents aborted mid-task with NIM 429 ("Request rejected — Provider rate limit reached") — unrecoverable per Failover Protocol. Recovered by direct Grep/Read on the source tree with line-number scoping (main-context verification). cmd-runner agent B (plan.md section capture) completed cleanly. No user-facing impact; verification completeness unaffected.

> **Open items unchanged by this pass:**
> - Production reload-via-NULL-pd gap (constraint #7 cardinal, held per #8): unchanged.
> - Orphaned native decls (`hasSeenDumpExtensions`/`extractKeylogFromPcapng`): defensive crash vector closed by `119baba0`; restore-vs-retire still pending lead pick.
> - Constraint #9/#10 (Restart-on-tunnel-construction-change): LANDED as constraint #10 via `3188160f` (2026-07-22); renumbered.
> - `#1462` block: still UNCOMMITTED in working tree (pre-existing, not authored by recent sessions).
> - Item (c) post-parse timing race: tracked-open per above; design decision (any "block earlier" change) deferred to lead direct engagement (constraint #8 release-blast-radius).

## Latest session — 2026-07-23 — NetGuard investigations CLOSED (architecture REFERENCE-ONLY, UI PARTIAL-ADOPTION shell-only); plan.md 3-edit `35e5cfd2` LANDED → PUSHED → CI GREEN; #1462 block left UNCOMMITTED; App Management shell AUTHORIZED NOT STARTED

> **Session scope — two relay-intent streams merged:** (1) Firewall UI Assessment (NetGuard as UX reference only, no code) — investigation-only, producing 6-section SOURCE-OF-TRUTH report with PARTIAL ADOPTION recommendation; (2) 2026-07-23 sign-off — close the NetGuard investigations in plan.md, record App Management shell open item. Both delivered; session closed.

> **NetGuard architecture verdict (REFERENCE-ONLY):** NetGuard is a packet FORWARDER with Java-side policy decisions (8 native callbacks); ADBye is capture + DPI filter with C-side rule matching (`blacklist_match_*`, nDPI). The native paradigms are incompatible — no native-layer merge. Only already-adopted cross-pollination: `getUidQ()` ([CaptureService.java:1728-1743](../app/src/main/java/com/adbye/filter/CaptureService.java#L1728-L1743)). Aggregate fit across 7 dimensions: **1 genuine / 4 zero-fit / 2 partial-reference / 1 N/A**. The informal "3/5 fit" superseded as overstating.

> **NetGuard UI/UX verdict (PARTIAL ADOPTION of per-app grid SHELL only — referenced in plan.md):** adoptable presentation shell (expandable app row, icon/name/state-color/hosts-badge/expander, Filterable search, sort name|uid, "changed" tint, inline-in-place expand, quick actions) — backed entirely by ADBye's EXISTING `MatchList` APP rules + `Blocklist` per-app-allowlist + `AppsLoader` patterns. **Zero** native/JNI/CaptureService/FilterListManager touch. REJECTED: WiFi/Mobile/Screen/Roaming/Lockdown/Apply/Notify dimension toggles + nested per-host access log (no native lever in ADBye's UID/domain-keyed interface-agnostic firewall gate; inert = fake-green per constraints #3/#4, or Phase-3+ backend feature with constraint-#2 SYN-drop risk). Recorded as Phase-3+ not authorized/scheduled.

> **plan.md 3-edit commit — LANDED, PUSHED, CI GREEN.** Committed as `2076c1f8` → subject-only-amended to `35e5cfd2` per lead request (subject: "close NetGuard investigations, record App Management shell scope"; body+tree preserved). Surgical: **22 insertions, 2 deletions** — the 3 reviewed hunks only (Master Plan Index correction [hunk1]; NetGuard verdict + App Management shell subsections [hunkA]; Phase-3 filterHttps annotation [hunk3]). Pushed: `95a13db7..35e5cfd2 fast-forward` to `origin/debug-emulator`. CI: Android CI/CD run `30073476732` — **ALL JOBS SUCCESS** (Unit Tests + Build APK + E2E Tests Emulator). Doc-only commit — no Java/XML/C touched — so E2E reproduced the prior baseline.

> **Bundling incident (caught, corrected, local-only):** `git commit <pathspec> plan.md` does an IMPLICIT `git add` of the working-tree file, bundling the pre-existing #1462 block into the commit (stat was 27 ins, not the authorized 22). Caught at verify-staged-diff-not-trust gate. The bad commit was LOCAL-ONLY (push held by constraint #8) — so no force-push: reset index for plan.md to parent, re-staged 3 hunks via `git apply --recount --cached`, and amended WITHOUT a pathspec (commits the index, NOT the working tree). Post-amend: `#1462 in HEAD:plan.md`=0, `NetGuard`=6, stat=22/2, working tree +5 (hunk2 preserved uncommitted). Re-learned for next session: to stage a version of a file WITHOUT also pulling in the working tree's extra edits, bypass `git add` entirely and use `git apply --cached` with a patch, or `git update-index --cacheinfo` with a manually-constructed blob.

> **#1462 block (pre-existing, NOT authored this session — left per lead directive):** uncommitted in working tree (` M plan.md`, +5). To be reviewed and landed as a separate commit in the next session. NOT to be bundled with anything else.

> **App Management shell — AUTHORIZED 2026-07-23, NOT started.** Plan.md record HAS landed (precondition met for future feature-branch work). Implementation is future-session task. ~600-900 LOC Java/XML estimated; constraint #8 applies. Explicit OUT list: all NetGuard dimension toggles + nested per-host access log.

> **Open items unchanged by this session:**
> - Production reload-via-NULL-pd gap (constraint #7 cardinal, held per #8): unchanged.
> - Orphaned native decls (`hasSeenDumpExtensions`/`extractKeylogFromPcapng`): defensive crash vector closed by `119baba0`; restore-vs-retire decision still pending lead pick.
> - Constraint #9 (Restart-on-tunnel-construction-change): PENDING REVIEW; wording agreed but NOT applied to plan.md.
> - TEMP-DIAG revert (`407df190` → `2a594d3e`): reverted and shipped prior session; CI green on that commit.

## Latest session — 2026-07-17 — MODE B mechanism VERIFIED per-attempt (6/6 attempt-instances, 2 independent runs); E2E RED = ORTHOGONAL httpbin.org flake on `testVpnConnectivity` only; **wider green-E2E gate still NOT cleared** (sole remaining blocker: `testVpnConnectivity` assertion, NOT consent-loss)

> **MODE B mechanism — VERIFIED, narrowly:** the per-attempt re-consent path targeted by `43007042` reached `isTunnelEstablished()=true` on EVERY attempt of the dispatch force-all run (`29484342034`) and the push run (`29484048872`). The user's working-discriminator bar — *"attempts 2 AND 3 hit isTunnelEstablished()=true on the consent path, not just no-timeout"* — is met on a 6-attempt evidence table. Full per-attempt anchors + "what this entry does NOT claim" scope section mirror the discipline of MODE A's "10/3/0 clarification" paragraph: see `fixed.md` "Commit `43007042`". This is the source-side disclosure of MODE B's mechanism; it is accurate and narrowly scoped regardless of how long CI stays red.

> **RED cause — ORTHOGONAL to MODE B (testVpnConnectivity is the only failing test, every attempt of every run):** the assertion at [AdbyeE2ETest.java:498](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L498) is `assertTrue("VPN should allow httpbin.org (got " + responseCode + ")", responseCode == 200 || 301 || 302)`. Per-attempt HTTP codes:

| Run | Attempt | HTTP code to `http://httpbin.org/get` |
|---|---|---|
| DISPATCH (29484342034) | 1 (09:02:04) | **503** |
| DISPATCH (29484342034) | 2 (09:04:34) | **504** |
| DISPATCH (29484342034) | 3 (09:06:53) | **503** |
| PUSH (29484048872) | 1 (08:58:28) | **503** |
| PUSH (29484048872) | 2 (09:01:09) | **-1** |
| PUSH (29484048872) | 3 (09:03:43) | **-1** |
>
> **Disambiguator — where the failure lives.** A DPI-block OR `prepare()`-consent-lost failure would yield `responseCode == -1` *with* `isTunnelEstablished()=false` at the upstream tunnel-up pre-assertion. What we see is the *opposite*: `isTunnelEstablished()=true` on every attempt of both runs (verified against the modeB / CaptureService anchors in the table above) followed by 503/504 (real HTTP-level responses — DNS resolved, TCP connected, request reached httpbin) OR -1 (no response). The 5xx codes in particular REQUIRE *the tunnel to be passing bytes to a remote server* — positive connectivity proof, not a tunnel failure. httpbin.org is a public rate-limited free service known to flake (5xx under load); same test, same code, passed in MODE A (run `29387922622`, 10 passed / 3 @Ignored / 0 failed — httpbin returned 200 then). Today's failures are mid-run flake on the **external endpoint**, not a regression introduced by MODE B's CI-workflow changes (which don't touch test source).

> **What this does and does NOT change at the higher level:**
>
> - **The green-E2E gate is still NOT cleared; the surface area narrowed, not widened.** Pre-session: 3 live blockers (MODE A probe-dead + MODE B consent-lost + testVpnConnectivity external-endpoint flake). Post-session: MODE A closed at `580a107f`; MODE B's mechanism verified by `43007042`'s 6/6; **the new sole blocker is `testVpnConnectivity`'s external-endpoint assertion**, scoped to its own commit (test-source blast radius, NOT MODE B's CI-workflow blast radius) per constraint #8. See `todo.md` Phase 1.b "testVpnConnectivity assertion fix" (this turn).
>
> - **`un-@Ignore`-ing the 3 still-`@Ignore`d tests** (`testAdBlockingViaVpn` / `testTrackingBlockingViaVpn` / `testSecurityBlockingViaVpn`) **is STILL gated on green E2E sustained** per `plan.md` Phase 1.b "SNI reload signal pending" + the `sBlReloadDone` gate. The gate's surface area changed but its CLOSED state did not: previously blocked on `consent-loss + probe-dead`; now blocked on `testVpnConnectivity`'s assertion shape (against httpbin.org). **Phase 1.b commit C is not landable until the test-source assertion fix in `todo.md` lands.**
>
> - **TEMP-DIAG instrumentation revert** still HELD per closing decision (a) of `57735774` (2026-07-14). Revert timing unchanged: own follow-up commit, gated on genuinely-green E2E — now = `testVpnConnectivity`-green in addition to all-current-blockers cleared.
>
> - **Production reload-via-NULL-pd gap** unchanged; still held constraint #8.

> **Worknotes updates this turn (mirror MODE A's two-stage shape, narrowly scoped):** `fixed.md` "Commit `43007042`" — per-attempt evidence + scope-discipline "NOT claiming." `progress.md` (this section) — narrative + the *gate-still-blocked* line. `todo.md` — new RESOLVED MODE B entry + new scoped test-source fix bullet + updated gate-wording on the existing TEMP-DIAG revert bullet. Narrowly written so it stays accurate regardless of how long CI stays red, and so future-me can't read it as broader than it is.

## Latest session — 2026-07-15 — MODE A GENUINELY CLOSED: rename `580a107f` → CI run `29387922622` **SUCCESS** (13 tests, 10 passed / 3 skipped / **0 failed** in 1m59s); zombie count **3→0**, self-termination **4→0**, timeouts **3→0**; first wait 60s→~4.8s, reuse waits ≤250ms; second-order sweep clean; the ~58s self-death was teardown-driven (false-positive zombie), NOT natural

> **Verified post-fix (artifact `e2e-580a107f/logcat-e2e.txt`, the green `connectedStandardDebugAndroidTest` attempt 04:22:30→04:23:20):** the lead's items 1-5 all came back clean, with one honest prediction correction.
>
> **Root cause confirmed + fixed:** `isCaptureEngineReady()` JNI probe was unbound since `2cfba946`; commit `580a107f` renamed the C symbol `Java_..._isCaptureEngineReady`→`Java_..._nativeIsCaptureEngineReady` ([jni_impl.c:881](app/src/main/jni/core/jni_impl.c#L881)) + updated the stale `@see` ([CaptureService.java:1154](app/src/main/java/com/adbye/filter/CaptureService.java#L1154)). Sibling audit: of 27 Java-native CaptureService methods, **only** this one mismatched — `nativeSetFirewallEnabled`/`nativeSetAdblockEnabled` correct. (Separate defect flagged: `hasSeenDumpExtensions`/`extractKeylogFromPcapng` ([L1983-1984](app/src/main/java/com/adbye/filter/CaptureService.java#L1983-L1984)) have NO C def anywhere — `extractKeylogFromPcapng` impl removed at `08ca34c2`; MainActivity-only, not E2E path → own commit.)
>
> | Lead item | Pre-fix (`57735774`, PID 3230) | **Post-fix (`580a107f`, PID 3123)** | Verdict |
> |---|---|---|---|
> | Engine-reach wait Δ (prepare→global_pd) | <110 ms | <110 ms | unchanged (init was never slow) |
> | **`waitForVpnTunnelEstablished` first-wait** | 60 s timeout (probe never true) | **~4.8 s** (cold-start: onCreate→establish→init_jni→~20 getIntPref→global_pd; poll catches at ≤250 ms after ready) | **60s → 4.8s** |
> | Reuse waits (tests 2-N, fast-path) | never fired (pre-fix fast-path unreachable) | **≤250 ms** (silent fast-path `serviceActive && engineReady` finally true; no prepare/teardown logged) | **60s → ≤0.25s, first time it works** |
> | **Zombie detections** | 3 (all false-positive) | **0** | **3 → 0** ✅ item-4 prediction exact |
> | False-positive teardown+cold-start tax | ~1-2 s × every reuse | **0** (single cold start, engine held alive through whole suite) | tax removed ✅ |
> | `Stopped packet loop` (~58 s self-termination) | **4** | **0** | was teardown-driven, NOT natural ✅ |
> | 60 s timeouts (`VPN tunnel not up`) | 3 | **0** | ✅ |
> | Success path taken (`post-wait isTunnelEstablished()=true`) | 0 | **1** | ✅ MODE A closed |
> | Gradle `connectedStandardDebugAndroidTest` | FAILED (3/3 attempts) | **BUILD SUCCESSFUL in 1m 59s** — 13 tests, 10 passed/3 `@Ignore`d skipped/0 failed | ✅ |
>
> **Honest correction to the pre-verification "~0.35 s near-instant" prediction:** that holds for REUSE waits (≤250 ms) but NOT the **first** wait. The first wait is **~4.8 s** — the engine's genuine cold-start cost on the CI emulator (prepare→`onCreate` 33.384→`onStartCommand` 33.635→VPN fd establish 37.682→init_jni 37.695→global_pd 38.005). Still a 12× improvement over 60 s and the load-bearing point (the probe now works), but the real first-wait number is ~4.8 s, not 0.35 s. Reported as-measured, not cherry-picked.
>
> **Second-order sweep (lead item 5):** CLEAN in this run. No FATAL/SIGSEGV/AssertionError/UnsatisfiedLinkError/AndroidRuntime-exception in the green attempt (the one `Exception` at L1368 is a RIL `RIL_REQUEST_GET_HARDWARE_CONFIG` radio-stack warning from an earlier FAILED attempt's PID 2756, pre-test-execution — env noise). The ADBye early-drop path **fired correctly** end-to-end: `early-drop [example.com]` ([L1494](e2e-580a107f/logcat-e2e.txt#L1494)) after `1 SNI loaded` ([L1490](e2e-580a107f/logcat-e2e.txt#L1490)); `early-drop [example.net]` ([L1533](e2e-580a107f/logcat-e2e.txt#L1533)) after `2 SNI loaded` ([L1525](e2e-580a107f/logcat-e2e.txt#L1525)) → the gate tests' drop assertions passed. The removed ~1-2 s teardown+cold-start tax did NOT unmask any race here. **Caveat (intellectual honesty): N=1 green.** Flake-by-definition may not show in one run; ≥2-3 green runs give real confidence. The MODE B consent-lost race is a live example of why N=1 isn't proof — it can skip (attempt-1 success here broke the retry loop before MODE B's reinstall-consent-loss could fire). Watch the next few runs; if a new flake appears it likely lives behind the now-removed tax.
>
> **What this RESOLVES beyond MODE A:** the ~57-59 s "recurring native self-termination races the poll" theory (progress.md 2026-07-15 banner's open question) is **refuted as a cause**: pre-fix there were 4 `Stopped packet loop` events at ~58 s cadence, but each was preceded by a false-positive ZOMBIE teardown at ~58 s (test poll-then-teardown cycle drove `stopPacketLoop`→`running=false`), NOT a native self-exit. Post-fix: 1 start, 0 stops — the engine does NOT self-terminate within the suite's ~40 s execution window. The "Path C: engine STOPPED ⇒ reuse dead tunnel" causal chain (fixed.md:80) is doubly wrong (probe-was-dead AND self-termination-was-teardown-driven).
>
> **HELD decisions UNCHANGED:** (a) TEMP-DIAG instrumentation still held (the native stamps confirmed engine-ready <110ms & teardown-driven self-termination; revert pending genuinely-green E2E sustained over ≥2-3 runs); (b) consent-lost race (MODE B) still DEFERRED — it did NOT fire this run (attempt-1 success → for-loop `break` skipped the reinstall cycle), which means MODE B remains UNTESTED-against this fix, still a live blocker when attempt-1 happens to fail; production reload-via-NULL-pd gap still constraint #8; 3 `@Ignore`d tests still gated on green E2E sustained. **Full verified entry: `fixed.md` "2026-07-15 — MODE A GENUINELY CLOSED (verified)".**

> **⚠ OVERTURNS the "MODE A residual mechanism undetermined" framing in the 2026-07-14 entry below AND fixed.md:327-333.** Both assumed `isCaptureEngineReady()` actually reads `global_pd != NULL`. It does **not**: the Java probe calls `nativeIsCaptureEngineReady()` ([CaptureService.java:1972](app/src/main/java/com/adbye/filter/CaptureService.java#L1972)) but the only C symbol is `Java_..._isCaptureEngineReady` ([jni_impl.c:880](app/src/main/jni/core/jni_impl.c#L880)) — wrong name, missing the `native` prefix. No `RegisterNatives` anywhere in the repo (zero native-side matches) ⇒ pure name-mangling ⇒ `UnsatisfiedLinkError` on every call ⇒ **caught at [CaptureService.java:1161](app/src/main/java/com/adbye/filter/CaptureService.java#L1161) ⇒ returns `false` unconditionally**. `isCaptureEngineReady=false` at the 60 s timeout carries **no information** about engine state; the whole Path A → Path C → "~58 s self-termination races the poll" arc chased a symptom of a different cause. Both halves born in the **same** commit `2cfba946` (git `-S` confirmed); the "load-bearing race-closing gate" it shipped has been inert since birth.
>
> **What this resolves:** MODE-A attempt-1 RED (PID 3230, consent GRANTED) — `waitForVpnTunnelEstablished` gates on `isTunnelEstablished() && isCaptureEngineReady()` ([AdbyeE2ETest.java:155-156](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L155-L156)); the latter is always false ⇒ never exits ⇒ 60 s timeout **regardless of engine state**. CI logcat (`e2e-57735774/logcat-e2e.txt`) proves `global_pd_assigned` fires <110 ms per cold start and holds ~58 s — a working probe would return at ~T+0.35 s. The ~57-59 s `Stopped packet loop` re-arm cycle (TIDs 3261/3266/3269/3275) is **not** the failure cause.
>
> **Caveats (also overturn):** (1) `2cfba946` did NOT close the race it claimed. (2) `57735774`'s "3/3 GREEN zombie recovery" validated teardown/restart *mechanics* but against a broken detector — `serviceActive && engineReady` ([L208](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L208)) is never true; `serviceActive && !engineReady` ([L220](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L220)) is always taken, so every active service registered as a zombie. (3) Path C's "engine STOPPED ⇒ reuse dead tunnel" inference is invalid — the probe never returned true, so its false isn't evidence of a stop. The 144 ms init_jni measurement itself still stands.
>
> **Minimal fix (release-visible native change, its own commit + explicit sign-off per constraint #8 — NOT applied this turn):** rename [jni_impl.c:880](app/src/main/jni/core/jni_impl.c#L880) symbol to `Java_com_adbye_filter_CaptureService_nativeIsCaptureEngineReady` (body unchanged). Orthogonal to the held items below.
>
> **Held decisions UNCHANGED:** (a) TEMP-DIAG instrumentation still held pending green E2E; (b) consent-lost race (MODE B, attempts 2-3) still DEFERRED — the probe fix does NOT touch MODE B's `prepare()`-returns-non-null path; production reload-via-NULL-pd gap still held constraint #8; 3 un-`@Ignore`d tests still gated on green E2E (MODE A blocker now removable, MODE B still gates). **Full entry: `fixed.md` "2026-07-15 — MODE A residual RESOLVED by source".**

### Action taken this session (lead-authorized 2026-07-15, in-flight)

1. **Verification paste (lead's pre-sign-off request) — all confirmed:**
   - Verbatim C sig `Java_com_adbye_filter_CaptureService_isCaptureEngineReady` ([jni_impl.c:881](app/src/main/jni/core/jni_impl.c#L881)) ≠ Java native decl `nativeIsCaptureEngineReady()` ([CaptureService.java:1972](app/src/main/java/com/adbye/filter/CaptureService.java#L1972)); wrapper swallows `UnsatisfiedLinkError`→false ([CaptureService.java:1158-1165](app/src/main/java/com/adbye/filter/CaptureService.java#L1158-L1165)).
   - `grep -rn "RegisterNatives\|JNI_OnLoad" app/src/main/jni/` → **exit 1, zero matches** (pure name-mangling; no remap table possible).
   - **Zombie-teardown × startVpnForTest-already-active cross-check (CI run `29317902504`, all 3 attempts/PIDs):** already-active startVpnForTest calls (=`ZOMBIE detected`) **= 3** (all on PID 3230, consent granted); teardown+restart cycles reaching `global_pd_assigned` **= 3**. **MATCH exactly (3 == 3).** PIDs 3430/3590 (consent-LOST, attempts 2-3) had **0** ZOMBIE detections — each consent-lost `run()` tears down (mCaptureThread=null) within ~1.5s, so the next ~60s-later call finds service inactive → cold path, never zombie. So pre-fix the "3/3 recoveries" were empirically real & complete, but every one was a *false-positive teardown of a healthy engine* (the broken probe made `engineReady` always false).
2. **Sibling audit (lead's item 2), repo-wide + per-line:** 27 Java-native CaptureService methods, **exactly one** C symbol name-minus-Java-name mismatch — this one. `nativeSetFirewallEnabled`/`nativeSetAdblockEnabled` carry the `native` prefix correctly. **Different defect found (flagged, out of scope):** `hasSeenDumpExtensions` + `extractKeylogFromPcapng` ([CaptureService.java:1983-1984](app/src/main/java/com/adbye/filter/CaptureService.java#L1983-L1984)) have **no C definition anywhere in the repo** (`extractKeylogFromPcapng` impl removed at `08ca34c2` "remove duplicate and malformed definition", collateral of the `32097972` package-rename). Both → `UnsatisfiedLinkError` when `MainActivity` calls them ([L733](app/src/main/java/com/adbye/filter/activities/MainActivity.java#L733), [L1189](app/src/main/java/com/adbye/filter/activities/MainActivity.java#L1189)); **not on the E2E androidTest path** → own commit.
3. **Rename applied (lead's item 1):** [jni_impl.c:881](app/src/main/jni/core/jni_impl.c#L881) `isCaptureEngineReady`→`nativeIsCaptureEngineReady` (body unchanged) + stale [CaptureService.java:1154](app/src/main/java/com/adbye/filter/CaptureService.java#L1154) `@see` updated. Diff = 2 lines. Local gradle guard `:app:testStandardDebugUnitTest :app:compileStandardDebugAndroidTestJavaWithJavac --rerun-tasks` → **BUILD SUCCESSFUL 1m 41s** (native symbol exercised by CI connectedAndroidTest). **Committed `580a107f`, pushed** (`57735774..580a107f` → `bbmtab/PCAPdroid` debug-emulator).
4. **Lead-item-4 prediction (zombie-recovery re-count, post-fix, PENDING CI):** pre-fix `ZOMBIE detected`=3 (all false-positive). Post-fix, near-instant waits → suite completes well under the ~57s native self-termination → tests reuse one engine via the (now-firing-for-first-time) fast-path `serviceActive && engineReady` → prediction: **zombie count → 0 (or near-0)**, teardown count → 1 (initial cold start). The genuine discriminator finally gets a fair test — report the REAL number.
5. **Lead-flagged second-order risk (PENDING CI):** pre-fix every reuse paid an unaccounted ~1-2s teardown+cold-start tax (false-positive zombies); post-fix reuses skip it → some tests genuinely faster for the first time → watch for a race that only stayed hidden behind that accidental delay. Report any new flake.

**CI result: ✅ SUCCESS.** Verified outcome: zombie **3→0** (lead item-4 prediction exact), first wait 60s→**~4.8s**, reuse waits ≤250ms (fast-path fires for first time), **0 failed / 13 tests (10 passed + 3 skipped) in 1m59s** (`connectedStandardDebugAndroidTest` BUILD SUCCESSFUL, attempt-1 broke the retry loop → MODE B never fired this run). Second-order sweep clean (N=1; watch next runs). ~58s self-termination was teardown-driven (4Stops→0), NOT natural — the "Path C: engine stopped" causal chain is refuted. MODE A **genuinely closed**. See header table above + fixed.md verified entry.

## 2026-07-14 — `57735774` zombie fix VERIFIED 3/3 in CI `29317902504`; E2E RED on DISJOINT deferred consent-lost race (NOT a regression); thread closed on disjointness

> **⚠ ROLLUP CORRECTION (2026-07-15, supersedes the 2026-07-14 close-out below).** The authoritative CI gradle/orchestrator rollup (fetched from job `87037081112` via `gh run view --job 87037081112 --log`, NOT the user-relayed line) overturns two claims in the 2026-07-14 framing:
> 1. **3 tests FAILED every attempt, not 1.** `testAdblockGateArmedFiresOnEarlyDrop` ([L535](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L535)), `testAdblockAllowlistExemptsBlockAtSyn` ([L712](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L712)), AND `testVpnConnectivity` ([L488](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L488)) — all throw at `waitForVpnTunnelEstablished` ([L169](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L169)). `testVpnConnectivity` (un-`@Ignore`'d in `9651fbbf` as the safe baseline test) now FAILS → real VPN-start-path regression. Per attempt: **3 FAILED + 3 SKIPPED** (testAdBlockingViaVpn/testTrackingBlockingViaVpn/testSecurityBlockingViaVpn `@Ignore`d) + the rest ~7 PASSED.
> 2. **MODE A is NOT fully resolved; "RED solely MODE B / NOT a regression" is WRONG.** Attempt 1 ran with consent GRANTED (PID 3230) and STILL all 3 FAILED with the ZOMBIE shape `isTunnelEstablished=**true**, isCaptureEngineReady=false (slept 59500ms)` — the gate fix recovers pre-existing zombies at `startVpnForTest` entry (the 3 recoveries at [L1272/L1358/L1443](lc-29317902504/logcat-e2e.txt#L1272) are real), but 3 tests' initial `waitForVpnTunnelEstablished` polled 59.5 s seeing `isCaptureEngineReady=false` → the recurring engine self-termination (`pd_run` returns → `global_pd=NULL` at `jni_impl.c:735` ~57-59 s after each cold start) defeats the wait EVEN WITH consent. Attempts 2-3 (PIDs 3430/3590, consent lost post-reinstall) FAILED with `isTunnelEstablished=**false**, slept 60000ms` → MODE B.
> **The two modes ARE independent (disjointness code-check below still holds — zombie branch structurally unreachable from consent-lost path) but they COMPOUND, not "MODE A green / MODE B red." Attempt-1 RED is MODE-A residual; attempts 2-3 RED is MODE B. Full entry + rollup table + next-step routing: `fixed.md` "Commit `57735774`" ⚠ correction banner.**
> **Fork re-frame:** the consent-lost single-attempt fix would NOT by itself green E2E — attempt 1 (consent granted) still fails 3 with zombie shape. MODE A residual (does the cold-started engine reach + hold `isCaptureEngineReady=true` through the test's wait, or does recurring ~57 s self-termination race it?) needs its OWN investigation before any fix shape is picked. Exact per-test mechanism undetermined from this artifact.

---

### Sequence of work this session

| # | Commit | Scope | Status |
|---|--------|-------|--------|
| 1 | `407df190` | test(ci): TEMP log init_jni phase boundaries + widen logcat filter to `pcapdroid-native:*` | PUSHED prior session; CI run `29234467941` executed → Path C RESOLVED (init_jni 144 ms, NOT 60 s — candidates 1 & 2 REFUTED). Full entry: `fixed.md` "407df190 Path C RESOLUTION". |
| 2 | `57735774` | fix(test): gate `startVpnForTest` fast-path on `isCaptureEngineReady()` too + detect ZOMBIE (`isServiceActive() && !isCaptureEngineReady()`) → `CaptureService.stopService()` + bounded `isServiceActive()` poll (`ZOMBIE_TEARDOWN_TIMEOUT_MS=15_000`) → AssertionError on timeout → cold restart; + companion teardown-side TEMP-DIAG `Log.d` stamps in `CaptureService.run()` (L1352/1355/1358/1367/1371-1373/1375-1377/1381) + onDestroy(L653) | PUSHED 2026-07-14T15:23 +0700. CI run **`29317902504`** COMPLETED conclusion **`failure`** (~28 m; PR parallel `29317905985` same SHA, also `failure`). Full entry: `fixed.md` "Commit 57735774". |

### `57735774` outcome — two empirically DISJOINT failure modes in one CI run (1824-line logcat, `e2e-57735774/logcat-e2e.txt` ↔ local `lc-29317902504/logcat-e2e.txt`)

- **MODE A — targeted fix VERIFIED WORKING (3/3 GREEN internally, PID 3230):** all three zombie-recovery cases succeeded — bounded teardown **317 / 508 / 410 ms** (≪ 15 s cap), `prepare()` returned **`null`** (consent **SURVIVED** the explicit teardown) in all 3, cold restart reached `global_pd_assigned` within **~1.27-1.79 s** of the torn-down stamp. Sequence verbatim (all 3): `zombie-detected → stopService-called → teardown-done (≪15 s) → prepare() null → onCreate → onStartCommand → init_jni [49-155 ms] → global_pd_assigned → Starting packet loop → wait succeeds`. **No** bounded-timeout AssertionError fired. Detailed per-recovery table + sub-step TEMP-DIAG timing: `fixed.md` "MODE A" subsection. Site citations: [logcat L1272/L1296/L1297](lc-29317902504/logcat-e2e.txt#L1272) (recovery 1), [L1358/L1381/L1382](lc-29317902504/logcat-e2e.txt#L1358) (recovery 2), [L1443/L1467/L1468](lc-29317902504/logcat-e2e.txt#L1443) (recovery 3).
- **Refined mechanism statement (replaces the prior "L1342-L1355 stall" hedge):** the explicit teardown completes the full `CaptureService.run()` L1352→L1381 sequence in **317-520 ms across all 3 recoveries with NO pathological stall.** The hedged `mBlacklists.save at L1351-52 could stall` hypothesis **did NOT fire** — `mMalwareDetectionEnabled=false` ⇒ `mBlacklists.save()` skipped via the L1350 guard (the `after mBlacklists.save (mMalwareDetectionEnabled=false)` TEMP-DIAG stamp confirms). The longest single sub-step is the **native packet-loop drain** (`Stopped packet loop`: 237/134/119 ms) — `stopPacketLoop` setting `running=false`, `pd_run` noticing and breaking out — the explicit-teardown native drain, NOT the zombie's pre-intervention stall. The fix's value is **detect-zombie-at-gate + bounded teardown + cold restart**; there was no stuck teardown in this artifact to unjam.
- **MODE B — disjoint, deferred, root cause of the E2E RED (NOT a regression):** PIDs 3430 (4 calls) + 3590 (4 calls) ALL logged `prepare(ctx) returned non-null Intent (consent NOT granted)` ([L1565/1594/1623/1652](lc-29317902504/logcat-e2e.txt#L1565), [L1700/1729/1758/1787](lc-29317902504/logcat-e2e.txt#L1700)) — consent never recovered within those PIDs' lifetimes; each cold-started with `establish()=null`, no `mParcelFileDescriptor`, prompt run() teardown (~115 ms), 60 s timeout reporting **`isTunnelEstablished=false, isCaptureEngineReady=false (slept 60000ms)`** ([L1681](lc-29317902504/logcat-e2e.txt#L1681), [L1816](lc-29317902504/logcat-e2e.txt#L1816)) — diagnostic-distinct from MODE A's `isTunnelEstablished=true` (zombie) and from Path A's `(true, false, 59500ms)`. Mechanism: `fixed.md:297` + `instrumented-test-vpn-design` memory — gradle 3× retry loop force-stops + reinstalls `app-debug.apk` between `connectedAndroidTest` attempts → reinstall clears the `prepare()`-registered prepared-VPN-holder consent (appops ACTIVATE_VPN does NOT register the holder).
- **Actual `FAIL` attribution (per user-relayed gradle runner output):** `testAdblockAllowlistExemptsBlockAtSyn[emulator-5554 - 11] FAILED`. Source-verified fail-fast shape: [AdbyeE2ETest.java:709](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L709) has NO try/catch around `waitForVpnTunnelEstablished()` at [L712](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L712) nor `assertTrue(...isTunnelEstablished()...)` at [L714](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L714) → uncaught AssertionError → gradle FAILED. Contrast: [testToggle... at L1099](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L1099) catches `AssertionError` at [L1122-L1124](app/src/androidTest/java/com/adbye/filter/AdbyeE2ETest.java#L1122-L1124) → graceful `skipping` message at [L1530](lc-29317902504/logcat-e2e.txt#L1530) / [L1681](lc-29317902504/logcat-e2e.txt#L1681) / [L1816](lc-29317902504/logcat-e2e.txt#L1816) (≠ RED contributor). Direct CI junit-XML fetch not completed in-session (`gh api` 404 on redacted repo path; artifact-list call timed out) — FAIL attribution is user-relayed from chat relay, flagged per the standing relay-content rule; aligns with the source-code fail-fast shape.

### Disjointness code check (user-requested thread-close pre-condition — CONFIRMED)

`isServiceActive()` body ([CaptureService.java:959-962](app/src/main/java/com/filter/CaptureService.java#L959-L962)) = `INSTANCE != null && INSTANCE.mCaptureThread != null`. `INSTANCE` sole write site [L226](app/src/main/java/com/filter/CaptureService.java#L226) (`onCreate`); deliberately not cleared at [L657](app/src/main/java/com/filter/CaptureService.java#L657) — resets to `null` ONLY on classloader unload (process death). On gradle reinstall → fresh process → fresh classloader → `INSTANCE=null` → `serviceActive=false` regardless of engine state → zombie-detect `serviceActive && !engineReady` short-circuits to `false` → falls through to cold-start, NOT zombie-teardown. The two modes are **structurally disjoint at the `startVpnForTest` gate** — the zombie branch is unreachable from the consent-lost code path. Full derivation: `fixed.md` "Disjointness code check".

### Closing-thread decisions (user-confirmed 2026-07-14 — source-of-truth for next session)

- **(a) Hold ALL TEMP-DIAG instrumentation** (native `407df190` init-jni-lookup stamps + Java `57735774` teardown stamps + `.yml` `pcapdroid-native:*` widening) **until E2E is genuinely green.** Reverting diagnostic coverage while E2E is RED on the disjoint race would mean re-adding it later if the consent-lost investigation needs it — pure waste. Held on-branch; revert will be its own clean commit per `8d722388 → 071129e6` packaging.
- **(b) Consent-lost race stays DEFERRED** — separate scoping decision for a later session, NOT picked up now. Two fix-shape options, different blast radii → separate scopes per constraint #8: (1) **single `connectedAndroidTest` attempt** (CI-workflow change — modify retry loop to not reinstall between attempts); (2) **preserve prepared-VPN-holder consent across reinstall** (test-lifecycle change — instrument-time consent or runner config retaining install state). User-named, user-stratified; explicit pick-up required.
- **The `TEMP-DIAG revert` ↔ `E2E genuinely green` ↔ `consent-lost resolution` coupling: revert needs green; green needs consent-lost resolved; consent-lost resolved needs explicit user scoping pick-up.** All three held at session close.
- Production reload-via-NULL-pd gap (`fixed.md:255-257` + `todo.md` Phase 1.b) — **UNCHANGED; HELD per constraint #8** (own commit cycle, explicit sign-off; release-visible production wrapper). `isCaptureEngineReady()` from `2cfba946` is the natural fix-candidate; gets its own commit when the user picks it up.
- Un-`@Ignore`-ing the 3 still-ignored tests (`testAdBlockingViaVpn`, `testTrackingBlockingViaVpn`, `testSecurityBlockingViaVpn`) — **UNCHANGED; gated on `sBlReloadDone` signal** (plan.md Phase 1.b "SNI reload signal pending"); transitively gated on green E2E (depends on consent-lost resolution). Phase 1.b commit C not landable until both gates clear.

### Status at session close (2026-07-14)

Thread **CLOSED** on the disjointness confirmation. No further action on consent-lost until explicitly picked up separately. TEMP-DIAG held on-branch per (a). The `57735774` zombie fix is verified working (3/3); the E2E RED is the separate, already-documented consent-lost race (`fixed.md:297`) — accepted as unrelated to this fix.

---

## 2026-07-13 — Path A red twice, starting Path C TEMP-DIAG instrumentation

### Sequence of work this session

| # | Commit | Scope | Status |
|---|--------|-------|--------|
| 1 | `2cfba946` | harness: poll `isCaptureEngineReady()` alongside `isTunnelEstablished()` in `waitForVpnTunnelEstablished` | PUSHED, CI completed (2 attempts both FAILED at same 60s timeout point) |
| 2 | `407df190` | test(ci): TEMPORARY log init_jni phase boundaries + widen logcat filter to stream pcapdroid-native tag | PUSHED, CI in flight (Debug job SUCCESS, Android CI/CD in_progress) |

### Path A rerun outcome (deterministic failure, not transient)

- **First attempt** (`29223116489`, attempt 1, 2026-07-13T04:30:19Z): at waitForVpnTunnelEstablished timeout (60s), test reported `isTunnelEstablished=true, isCaptureEngineReady=false (slept 59500ms)` — VPN fd came up cleanly at 04:31:54.462 (~3s after onStartCommand), but capture engine never initialized within the 60s harness window. PID 4579 stayed continuously alive; zero FATAL EXCEPTION / SIGSEGV for app process.
- **Second attempt** (`29223118723`, attempt 2, 2026-07-13T05:46:58Z): **IDENTICAL timeout-point smoking gun** — `isTunnelEstablished=true, isCaptureEngineReady=false (slept 59500ms)` at 05:34:53.265. Same 60s window from VPN fd up (05:30:56.449) to timeout. PID  PID 2979 stayed continuously alive; zero FATAL EXCEPTION / SIGSEGV for app process. Two independent attempts landing on the same gap with the same timing = NOT a transient env-flake. Per user direction: skip Path B (timeout bump) entirely, go straight to Path C.

### What Path C instruments (TEMP-DIAG, reverts in follow-up)

- **jni_impl.c**: Added `log_d("[TEMP-DIAG init-jni-lookup] …")` stamps at:
  - init_jni entry (line 535)
  - after class lookups done (lines 552-555)
  - method-ID lookups start (line 562-565)
  - method-ID lookups MIDPOINT (~15 of 29 lookups) (lines 583-586)
  - method-ID lookups done (lines 603-606)
  - field lookups done (lines 619-622)
  - enum lookups done / init_jni RETURN (lines 630-633)
  - runPacketLoop entry before init_jni(env) (line 651)
  - after global_pd = &pd assignment (engine-ready anchor) (lines 725-727)
- **.github/workflows/android-ci.yml**: Widened logcat filter in connectedStandardDebugAndroidTest step to include `pcapdroid-native:*` so diagnostic stamps land in the artifact (was: CaptureService:* VpnService:* AndroidRuntime:* System.err:* ActivityManager:* → now adds pcapdroid-native:*)
- **Purpose**: Disambiguates WHERE in init_jni's sequence the 60s accumulates — specifically per user sharpening capturing timing of the ~50 jniGetMethodID calls (jni_impl.c:557-600) — to distinguish between:
  1. Wider init tail-latency than characterized (candidate 1: environmental, harmless)
  2. Genuine JNI hang in initialization (candidate 2: classloader/method-ID lookup stall)
  3. CI emulator wedge unrelated to engine init (candidate 3: PMS/system_server starvation)
- **Local verification**: clang -fsyntax-only on jni_impl.c (no warnings/errors)

### Path A queued at session close
- `gh run rerun 29223118723 --failed` accepted, rerun API status `run_attempt=2, in_progress`. Outcome recorded above (2 failures).

### Status
- **Path C RESOLVED (2026-07-14).** CI run `29234467941` (descends from `407df190`; 9 TEMP-DIAG stamps present in `lc-pathc-29234467941/logcat-e2e.txt`). init_jni ENTRY→RETURN = **144 ms** (NOT 60 s) — candidates 1 (init tail-latency) & 2 (JNI lookup hang) from `fixed.md:270-273` REFUTED. `global_pd_assigned` fired at 08:26:23.581 (PID 2990); engine ran ~2 min, then `pd_run()` returned (`jni_impl.c:735` → `global_pd = NULL`) **without a logged `stopService`** while `sTunnelEstablished` stayed true. The 60 s timeout's `isCaptureEngineReady=**false**, isTunnelEstablished=**true**` is the engine having STOPPED, not slow init. Fix points at `startVpnForTest`'s `isServiceActive()` fast-path (`AdbyeE2ETest.java:199`) reusing a tunnel whose native pd_run has died — gate the fast-path on `isCaptureEngineReady()` too, not init timeout. Supersedes `todo.md:56`'s narrower `mCaptureThread` guard (insufficient — native `global_pd`-NULL isn't visible there). Full entry: `fixed.md` "407df190 Path C RESOLUTION" subsection. **Cross-run caveat:** Path A reruns (PIDs 4579/2979) had `pcapdroid-native:*` filtered out, so their mechanism can't be directly confirmed — only inferred. This lands on "needs fixing" (service-restart reuse race), NOT "real work taking real time" (the AdGuard-precedent reading would predict ~30 s of genuine filter-load; init was 144 ms).
- TEMP-DIAG instrumentation (`407df190`) revert still owed as its own follow-up commit (same packaging as `8d722388 → 071129e6`).
- Production reload-via-NULL-pd gap remains tracked in `todo.md` and `fixed.md` per constraint #8 (own commit cycle required).